// Data has been migrated to Supabase. This file is kept only as a backwards-
// compatible re-export of the FuelStation type so older imports keep working.
// New code should import from '@/lib/types'.
export type { FuelStation } from './types';
